export const MODEL='google/gemini-2.5-flash';export async function callOpenRouter(apiKey,messages){const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json','HTTP-Referer':location.origin,'X-Title':'Thai Quest'},body:JSON.stringify({model:MODEL,messages,temperature:0.8})});if(!r.ok)throw new Error('OpenRouter error '+r.status);const d=await r.json();return d.choices&&d.choices[0]&&d.choices[0].message&&d.choices[0].message.content||''}export function parseJsonObject(t){const s=t.trim().replace(/^```json\s*/i,'').replace(/```$/i,'').trim();const a=s.indexOf('{');const b=s.lastIndexOf('}');if(a===-1||b===-1)throw new Error('Model did not return JSON');return JSON.parse(s.slice(a,b+1))}