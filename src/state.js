const VOCAB_KEY='tq_vocab_records';const LEGACY_VOCAB_KEY='tq_vocab';export const REVIEW_INTERVALS=[0,1,2,4,7];export function loadVocabRecords(){const modern=safeJson(localStorage.getItem(VOCAB_KEY),null);if(Array.isArray(modern))return new Map(modern.map(i=>[i.thai,normalize(i)]));const legacy=safeJson(localStorage.getItem(LEGACY_VOCAB_KEY),[]);if(Array.isArray(legacy)){return new Map(legacy.map(t=>[t,normalize({thai:t})]))}return new Map()}export function saveVocabRecords(r){localStorage.setItem(VOCAB_KEY,JSON.stringify([...r.values()]))}export function getDueReview(r,c,l){return[...r.values()].filter(i=>i.nextReviewChapter<=c).sort((a,b)=>a.nextReviewChapter-b.nextReviewChapter||a.correctCount-b.correctCount).slice(0,l||3)}export function upsertSeenVocab(r,i,c){const cur=r.get(i.thai)||normalize(i);const seen=cur.seenCount+1;r.set(i.thai,normalize({...cur,...i,seenCount:seen,lastSeenChapter:c,nextReviewChapter:c+REVIEW_INTERVALS[Math.min(seen,REVIEW_INTERVALS.length-1)]}));saveVocabRecords(r)}export function markVocabAnswer(r,thai,ok,c){const cur=r.get(thai);if(!cur)return;const cc=cur.correctCount+(ok?1:0);const step=ok?Math.min(cc+1,REVIEW_INTERVALS.length-1):1;r.set(thai,normalize({...cur,correctCount:cc,lastSeenChapter:c,nextReviewChapter:c+REVIEW_INTERVALS[step]}));saveVocabRecords(r)}export function createInitialState(){return{apiKey:localStorage.getItem('tq_apiKey')||'',kidName:'',interests:[],chapter:Number(localStorage.getItem('tq_chapter')||'1'),xp:Number(localStorage.getItem('tq_xp')||'0'),level:localStorage.getItem('tq_level')||'beginner',storySummary:localStorage.getItem('tq_story_summary')||'',currentChapter:null,vocabRecords:loadVocabRecords()}}export function persistProgress(s){localStorage.setItem('tq_xp',String(s.xp));localStorage.setItem('tq_chapter',String(s.chapter));localStorage.setItem('tq_story_summary',s.storySummary||'');saveVocabRecords(s.vocabRecords)}function normalize(i){return{thai:i.thai||'',meaning:i.meaning||'',romanization:i.romanization||'',type:i.type||'word',example:i.example||'',seenCount:Number(i.seenCount||0),correctCount:Number(i.correctCount||0),lastSeenChapter:Number(i.lastSeenChapter||0),nextReviewChapter:Number(i.nextReviewChapter||1)}}function safeJson(v,f){try{return JSON.parse(v)}catch{return f}}