export function speakThai(text){speak(text,'th-TH',0.82)}export function speakStory(text){speak(text,'en-US',0.92)}export function stopAudio(){if(window.speechSynthesis){window.speechSynthesis.cancel()}}export function wireAudioDelegation(root){(root||document).addEventListener('click',function(e){var t=e.target.closest('[data-speak]');if(!t)return;speakThai(t.dataset.speak)})}function speak(text,lang,rate){if(!window.speechSynthesis||!text)return;var u=new SpeechSynthesisUtterance(text);u.lang=lang;u.rate=rate;window.speechSynthesis.cancel();window.speechSynthesis.speak(u)}