import{createInitialState,persistProgress,upsertSeenVocab,markVocabAnswer,markSpeechAttempt,hasChapterCheckpoint,clearChapterCheckpoint}from'./state.js';import{speakStory,stopAudio,wireAudioDelegation}from'./audio.js';import{fallbackChapter,generateChapter}from'./story.js';import{listenThaiOnce,scoreThaiSpeech,scoreShadowLine,scoreSpokenChoice,renderDiffHtml,supportsSpeechPractice}from'./speech.js';const s=createInitialState();const $=q=>document.querySelector(q);$('#apiKey').value=s.apiKey;wireAudioDelegation(document);$('#interests').addEventListener('click',e=>{const b=e.target.closest('[data-interest]');if(b)b.classList.toggle('selected')});$('#startBtn').addEventListener('click',start);async function start(){s.apiKey=$('#apiKey').value.trim();s.kidName=$('#kidName').value.trim()||s.kidName||'Friend';s.interests=[...document.querySelectorAll('[data-interest].selected')].map(b=>b.dataset.interest);if(!s.apiKey){$('#setupError').textContent='Please paste your OpenRouter API key.';return}localStorage.setItem('tq_apiKey',s.apiKey);$('#setup').style.display='none';$('#story').style.display='block';updateStats();await load()}async function load(){const c=$('#storyContent');c.innerHTML='<div class="card">🪬 Writing your quest...</div>';if(hasChapterCheckpoint(s)){// resume
 render();return}
s.unlockedScenes=1;s.bossScore=0;try{s.currentChapter=await generateChapter(s)}catch(e){console.warn(e);s.currentChapter=fallbackChapter(s)}for(const it of s.currentChapter.targetVocab){upsertSeenVocab(s.vocabRecords,it,s.chapter)}s.storySummary=s.currentChapter.summary;persistProgress(s);render()}function difficulty(){if(s.speechStreak>6)return .75;if(s.speechStreak>3)return .65;return .55}function isBossChapter(){return s.chapter%3===0}function render(){const c=s.currentChapter;$('#storyContent').innerHTML=`<article class="scene"><div class="scene-emoji">${isBossChapter()?'🐉':'🪬'}</div><h2>${isBossChapter()?'Boss Challenge: ':''}${esc(c.title)}</h2><div class="actions"><button class="btn-secondary" id="read">🔊 Read</button><button class="btn-secondary" id="stop">⏹ Stop</button></div>${tray(c)}</article>${c.scenes.map((sc,i)=>scene(sc,i)).join('')}${shadowSection(c)}${c.lessons.map((l,i)=>lesson(l,i)).join('')}${speakSection(c)}${bossSection(c)}<div class="actions"><button class="btn-primary" id="next">${isBossChapter()?'Claim Reward ✨':'Next ✨'}</button></div>`;$('#read').onclick=()=>speakStory(c.chapterNarration||collect(c));$('#stop').onclick=stopAudio;$('#next').onclick=()=>{clearChapterCheckpoint();s.chapter++;s.xp+=isBossChapter()?25:10;persistProgress(s);updateStats();load()};document.querySelectorAll('[data-lesson]').forEach(wireLesson);document.querySelectorAll('[data-voice-answer]').forEach(wireVoiceAnswer);document.querySelectorAll('[data-speak-practice]').forEach(wireSpeakPractice);document.querySelectorAll('[data-shadow]').forEach(wireShadow);document.querySelectorAll('[data-boss-word]').forEach(wireBossWord);document.querySelectorAll('[data-boss-line]').forEach(wireBossLine);updateStats()}/* rest unchanged */